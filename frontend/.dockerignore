# Ставится заново внутри образа — Linux-бинарники из хостового node_modules
# (особенно на Windows) всё равно не заработают в alpine-контейнере.
node_modules/

# Результат сборки — образ собирает его сам через npm run build.
dist/

.git/
.gitignore
